---
paths:
  - src/lib/seo.ts
  - src/routes/index.tsx
  - src/routes/book.tsx
---

SEO / copy workstream. FAQ on the home page must match JSON-LD in `src/lib/seo.ts`. Phone belongs in the header and footer only (plus the door-hanger replica). Don't invent prices — they live in `pricebook.ts`.
